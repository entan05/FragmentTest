<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.team.e_works.fragmenttest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RootActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FragmentTest01Activity" />
        <activity android:name=".FragmentTest02Activity" />
        <activity android:name=".FragmentTest03Activity" />
        <activity android:name=".FragmentTest04Activity" />
        <activity android:name=".FragmentTest05Activity" />
        <activity android:name=".FragmentTest06Activity"></activity>
    </application>

</manifest>